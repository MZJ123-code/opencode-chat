# OpenCode Chat — 架构深度分析

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     用户浏览器                            │
│  ┌─────────────────────┐   ┌─────────────────────────┐  │
│  │  React 19 App       │   │  Vanilla JS Fallback    │  │
│  │  (Vite HMR @5173)   │   │  (public/index.html)    │  │
│  └────────┬────────────┘   └──────────┬──────────────┘  │
│           │ /api (proxy)              │ /api             │
└───────────┼───────────────────────────┼──────────────────┘
            │                           │
┌───────────▼───────────────────────────▼──────────────────┐
│                    Express Server (:3000)                  │
│                                                           │
│  Middleware Chain:                                         │
│    json → clientIP → requestLogger → rateLimiter → routes │
│                                                           │
│  Routes (/api/*):                                          │
│    sessions  messages  chat  events  feedback             │
│    stats  agents  permission  abort                        │
│                                                           │
│  Services:                                                 │
│    sessionService   statsService   analyticsService       │
│    userService   eventBuffer   opencode (SDK wrapper)     │
│                                                           │
│  Storage:                                                  │
│    ┌─────────────────┐   ┌──────────────────────┐        │
│    │  Memory (Map)    │   │  SQLite (bun:sqlite)  │        │
│    │  sessionMeta     │   │  analytics.db         │        │
│    │  ipUsers         │   │  page_visits          │        │
│    │  ipSessions      │   │  questions            │        │
│    │  stats           │   │  feedback             │        │
│    └────────┬────────┘   └──────────────────────┘        │
│             │                                             │
│    Logs (File): _stats.json + server.log                  │
│                                                           │
│  ┌─────────────────────────────────────────────────┐      │
│  │ OpenCode Subprocess (via @opencode-ai/sdk/v2)    │      │
│  │ createOpencode({ port: 4096, config })           │      │
│  │ → 自动管理 OpenCode 进程生命周期                   │      │
│  │ → client.session.prompt() / promptAsync()        │      │
│  │ → client.event.subscribe() (SSE 流)              │      │
│  └─────────────────────────────────────────────────┘      │
└───────────────────────────────────────────────────────────┘
```

## 请求流程

### 同步聊天

```
POST /api/chat  { sessionId, message, agent }
  → requireBody + requireSessionOwnership (middleware)
  → recordMessage + incrementQuestions + recordQuestion
  → client.session.prompt() (等待 OpenCode 完成)
  → 解析 parts, tokens, toolCalls
  → 返回完整 JSON { sessionId, reply, parts, tokens }
```

### 异步聊天（主流方式）

```
POST /api/chat/async  { sessionId, message, agent }
  → 同上校验 + 记录
  → client.session.promptAsync() (不等待)
  → 立即返回 { ok: true }

SSE /api/events (持续连接)
  → 订阅所有 Agent 目录的事件流
  → message.part.updated → 实时推送 Part 更新
  → message.part.delta → 文本增量
  → session.next.tool.* → 工具调用状态
  → session.next.reasoning.* → 推理过程
  → session.status → 会话状态变更
```

### 权限弹窗流程

```
Agent 需要执行敏感操作
  → OpenCode 发送 permission.asked / question.asked 事件
  → SSE 转发到前端
  → PermissionDialog 弹窗
  → 用户操作 → POST /api/permission/respond 或 /api/permission/question/reply
  → Express 调用 SDK 的 permission 响应接口
```

## 数据流图

### 会话生命周期

```
用户创建会话                       用户发送消息
    │                                  │
    ▼                                  ▼
POST /api/sessions              POST /api/chat/async
    │                                  │
    ├─ ensureIP(ip)                    ├─ requireBody("sessionId","message")
    ├─ createSession(ip, title)        ├─ requireSessionOwnership
    │   ├─ client.session.create()     │   ├─ validateOwnership(ip, sid)
    │   ├─ ipUsers.add(sessionId)      │   └─ tryRegisterSession() [fallback]
    │   ├─ ipSessions.push(sessionId)  ├─ recordMessage (递增消息数)
    │   └─ sessionMeta.set(sessionId)  ├─ incrementQuestions (内存 stats)
    └─ 返回 sessionId                  ├─ recordQuestion (SQLite)
                                        └─ client.session.promptAsync()
                                               │
                                    ╔═══════════════════════════╗
                                    ║  OpenCode 处理中 (Agent)  ║
                                    ║  SessionPrompt.loop()     ║
                                    ╚═══════════════════════════╝
                                               │
                          ┌────────────────────┼────────────────────┐
                          ▼                    ▼                    ▼
                   SSE 事件流             同步模式(POST /api/chat)
                   event.subscribe()       client.session.prompt()
                          │                    │ (等待完成)
                          ├─ message.updated    ├─ 解析 parts/tokens
                          ├─ message.part.delta └─ 返回完整 JSON
                          ├─ session.next.tool.*
                          ├─ session.status/error
                          └─ session.created/deleted
```

### 并发数据修改路径（风险点）

```
HTTP 路由                    SSE 事件流                    定时器
(POST /api/sessions)         (GET /api/events)              (setInterval)
       │                           │                            │
       │  createSession()          │  processEvent()            │ cleanupExpiredSessions()
       │                           │                            │
       ├── sessionMeta.set(id)     ├── sessionMeta.set(id)      ├── sessionMeta.delete(id)
       ├── ipUsers.get(ip).add     ├── ipUsers.get(ip).add      ├── ipUsers.get(ip).delete
       └── ipSessions.get(ip).push └── ipSessions.get(ip).push  └── ipSessions.get(ip).splice
                ▲                           ▲                           ▲
                └────────── 三者无锁并发修改同一 Map ──────────────────┘
```

**关键问题**：`sessionMeta`、`ipUsers`、`ipSessions` 三个 Map 被 HTTP 路由、SSE 事件处理、定时清理器同时读写，无任何并发控制。`ipSessions`（数组）和 `ipUsers.sessionIds`（Set）之间存在数据冗余，不一致风险高。

### SSE 事件类型完整列表（20+ 种）

| 事件类型 | 用途 | 前端处理 |
|---------|------|---------|
| `message.updated` | 消息元数据更新 | `onMessageUpdated` |
| `message.removed` | 消息被删除 | `onMessageRemoved` |
| `message.part.updated` | 消息片段更新（文本/工具推理） | `onPartUpdated` |
| `message.part.delta` | 文本增量推送 | `onPartDelta` |
| `message.part.removed` | 片段删除 | `onPartRemoved` |
| `session.updated` | 会话信息更新 | `onSessionUpdated` |
| `session.created` | 子会话创建 | `onSessionUpdated`（注册子会话） |
| `session.deleted` | 会话删除 | `onSessionDeleted`（清理本地） |
| `session.status` | 会话状态变更（running/idle/completed） | `onSessionStatus` |
| `session.idle` | 会话进入空闲 | `onSessionIdle` |
| `session.error` | 会话错误 | `onSessionError` |
| `permission.asked` | 权限请求 | `onPermissionAsked` |
| `question.asked` | 多步问答 | `onPermissionAsked` |
| `permission.updated` | 权限状态更新 | `onPermissionAsked` |
| `session.next.tool.called` | 工具开始执行 | `onToolCalled` |
| `session.next.tool.progress` | 工具执行进度 | `onToolProgress` |
| `session.next.tool.success` | 工具执行成功 | `onToolSuccess` |
| `session.next.tool.failed` | 工具执行失败 | `onToolFailed` |
| `session.next.reasoning.delta` | 推理过程增量 | `onReasoningDelta` |
| `session.next.reasoning.ended` | 推理结束 | `onReasoningEnded` |
| `session.next.shell.started` | Shell 命令开始 | `onShellStarted` |
| `session.next.shell.ended` | Shell 命令结束 | `onShellEnded` |
| `session.next.step.ended` | 步骤完成 | `onStepEnded` |

### SSE 多流订阅模型

```js
// server/routes/events.js:165-169
const subscribeTasks = [client.event.subscribe()]  // 默认目录
for (const dir of AGENT_DIR_MAP.values()) {         // 每个独立目录
  subscribeTasks.push(client.event.subscribe({ directory: dir }))
}
const streams = await Promise.all(subscribeTasks)
// 若 3 个 Agent 有独立目录 → 4 个长连接 / 客户端
```

每个客户端连接维护 N+1 个 OpenCode 事件订阅流（N = 配置了独立目录的 Agent 数量），所有流的事件通过 `processEvent()` 合并后转发给浏览器。

### 子会话导航

```
主会话 → Agent 使用 task 工具创建子会话
  → OpenCode 发送 session.created 事件 (含 parentID)
  → SSE processEvent() 检测到子会话 → sessionMeta 注册
  → 前端 ChatContext.onSessionUpdated 处理
     → sessionMetaRef 记录 parentID
     → taskCallToChild 映射 (靠顺序匹配 unmapped.shift())
  → 用户可点击子会话导航
  → navigateToSession(sessionId) 切换
  → navigateBack() / navigateToParent() 返回
  → 导航栈存在 ref 和 state 双存储 (navigationStackRef + navigationStack)
```

## 模块职责

### 服务端

| 模块 | 职责 | 关键文件 |
|------|------|---------|
| 应用入口 | 启动 OpenCode、初始化 DB、挂载中间件 | `server/index.js`, `server/app.js` |
| 配置 | 加载 `config.json` + 环境变量覆盖 | `server/config.js`, `server/config.json` |
| 路由注册 | 注册 9 个路由模块到 Express | `server/routes/index.js` |
| SDK 封装 | OpenCode 子进程生命周期管理 | `server/services/opencode.js` |
| 会话服务 | 会话 CRUD、IP 限额、TTL 清理 | `server/services/sessionService.js` |
| 统计服务 | 内存统计 + JSON 文件持久化 | `server/services/statsService.js` |
| 分析服务 | SQLite 数据分析记录 | `server/services/analyticsService.js` |
| 事件缓冲 | SSE 事件环形缓冲区 + TTL 清理 | `server/services/eventBuffer.js` |
| 用户服务 | IP 提取 + 访客注册 | `server/services/userService.js` |
| 数据库 | SQLite 表创建和实例管理 | `server/storage/database.js` |
| 内存存储 | Map 定义和 stats 结构 | `server/storage/store.js` |
| 日志 | 控制台 + 文件轮转 (10MB, 10 归档) | `server/logger/index.js` |

### 前端

| 模块 | 职责 | 关键文件 |
|------|------|---------|
| 应用入口 | React 渲染 + Provider 嵌套 | `client/src/main.tsx`, `App.tsx` |
| 聊天上下文 | 全局状态管理者 (793 行) | `client/src/contexts/ChatContext.tsx` |
| 事件 Hook | SSE 连接 + 20+ 事件类型分发 | `client/src/hooks/useEvents.ts` |
| API 层 | 按资源拆分的 HTTP 客户端 | `client/src/api/*.ts` |
| 聊天组件 | 消息渲染、输入、Agent 选择 | `client/src/components/chat/*` |
| 看板组件 | 统计图表、筛选、排序表格 | `client/src/components/dashboard/*` |
| 布局组件 | 可拖拽侧边栏、聊天区域 | `client/src/components/layout/*` |
| 通用组件 | 权限弹窗、错误边界、主题切换 | `client/src/components/common/*` |
