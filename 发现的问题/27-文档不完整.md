# 问题 27：文档不完整

## 严重程度
🟡 需要关注

## 问题描述

缺少 API 文档（JSDoc/TSDoc）、使用示例和最佳实践指南，不利于其他开发者理解和使用项目。

## 问题详情

### 当前文档状态

| 文档类型 | 状态 | 说明 |
|---------|------|------|
| README | ✅ 完整 | 项目介绍和快速开始 |
| AGENTS.md | ✅ 完整 | 编码规范和 API 路由 |
| API 文档 | ❌ 缺失 | 没有 OpenAPI/Swagger 规范 |
| 使用示例 | ❌ 缺失 | 没有完整的使用示例 |
| 最佳实践 | ❌ 缺失 | 没有使用指南 |

### 问题分析

1. **API 文档缺失**
   - 没有 OpenAPI 规范文档
   - 无法自动生成 API 文档
   - 前后端协作困难

2. **使用示例缺失**
   - 没有完整的使用示例
   - 没有各种场景的示例
   - 用户不知道如何开始使用

3. **最佳实践缺失**
   - 没有性能优化建议
   - 没有错误处理指南
   - 没有安全使用指南

## 影响

- 新开发者难以入门
- 前后端协作效率低
- 不利于社区贡献

## 改进建议

### 方案 A：添加 OpenAPI 规范

```yaml
# server/openapi.yaml
openapi: 3.0.0
info:
  title: OpenCode Chat API
  version: 1.0.0
  description: OpenCode Chat 服务 API 文档

paths:
  /api/sessions:
    get:
      summary: 获取会话列表
      parameters:
        - name: X-Forwarded-For
          in: header
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Session'

    post:
      summary: 创建会话
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'

components:
  schemas:
    Session:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        createdAt:
          type: string
          format: date-time
```

### 方案 B：添加 JSDoc 注释

```typescript
/**
 * 创建新会话
 * 
 * @param {Request} req - Express 请求对象
 * @param {Response} res - Express 响应对象
 * @param {NextFunction} next - Express 下一个中间件
 * 
 * @example
 * POST /api/sessions
 * Body: { "title": "New Session" }
 * Response: { "id": "abc123", "title": "New Session" }
 */
export async function createSession(req: Request, res: Response, next: NextFunction) {
  // ...
}

/**
 * 获取会话列表
 * 
 * @param {string} ip - 客户端 IP 地址
 * @returns {Session[]} 会话列表
 * 
 * @example
 * GET /api/sessions
 * Headers: { "X-Forwarded-For": "192.168.1.1" }
 * Response: [{ "id": "abc123", "title": "Session 1" }]
 */
export async function getSessions(ip: string): Promise<Session[]> {
  // ...
}
```

### 方案 C：添加使用示例

```markdown
# 使用示例

## 创建会话

```bash
curl -X POST http://localhost:3000/api/sessions \
  -H "Content-Type: application/json" \
  -H "X-Forwarded-For: 192.168.1.1" \
  -d '{"title": "My Session"}'
```

## 发送消息

```bash
curl -X POST http://localhost:3000/api/chat/async \
  -H "Content-Type: application/json" \
  -H "X-Forwarded-For: 192.168.1.1" \
  -d '{"sessionId": "abc123", "message": "Hello, AI!"}'
```

## 接收 SSE 事件

```javascript
const eventSource = new EventSource(
  'http://localhost:3000/api/events?ip=192.168.1.1'
);

eventSource.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Received event:', data);
};
```
```

### 方案 D：添加最佳实践指南

```markdown
# 最佳实践指南

## 性能优化

### 1. 使用异步 API

```typescript
// ✅ 推荐：使用异步 API
await fetch('/api/chat/async', {
  method: 'POST',
  body: JSON.stringify({ sessionId, message })
});

// ❌ 避免：使用同步 API（会阻塞）
await fetch('/api/chat', {
  method: 'POST',
  body: JSON.stringify({ sessionId, message })
});
```

### 2. 合理使用 SSE

```typescript
// ✅ 推荐：复用 SSE 连接
const eventSource = new EventSource('/api/events');

// ❌ 避免：每次请求都创建新连接
for (let i = 0; i < 10; i++) {
  new EventSource('/api/events');
}
```

## 错误处理

### 1. 使用 try-catch

```typescript
try {
  const response = await fetch('/api/sessions');
  const data = await response.json();
} catch (error) {
  console.error('Request failed:', error);
}
```

### 2. 检查响应状态

```typescript
const response = await fetch('/api/sessions');
if (!response.ok) {
  const error = await response.json();
  throw new Error(error.message);
}
```

## 安全使用

### 1. 验证 IP 地址

```typescript
// ✅ 推荐：验证 IP 地址
const ip = getClientIP(req);
if (!isValidIP(ip)) {
  return res.status(401).json({ error: 'Invalid IP' });
}
```

### 2. 使用 HTTPS

```typescript
// ✅ 推荐：生产环境使用 HTTPS
const server = https.createServer(options, app);
```
```

## 优先级

**P2（可以改进）**

- 文档不影响核心功能
- 可以在后续版本中逐步完善

## 相关文件

- `server/openapi.yaml`（需要创建）
- `server/routes/` 目录下的路由文件
- `README.md`
