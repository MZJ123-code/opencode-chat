# 问题解决跟踪

各问题的修复进展记录。

| # | 问题 | 严重度 | 状态 | 解决方式 | 日期 |
|:-:|------|:----:|:----:|----------|:----:|
| 01 | IP 作为用户身份标识不可靠 | 🔴 | <span style="color:green">已解决</span> | 新增 `server/middleware/userToken.js`，UUID Token + Cookie 标识用户；`req.userId` 替代 `req.clientIP` 做会话归属判断；前端 `client.ts` 自动携带 `x-user-token` 请求头 | 2026-06-12 |
| 02 | 数据持久化严重不足 | 🔴 | <span style="color:green">已解决</span> | `database.js` 新增 `sessions` 表；`sessionService.js` 的 `createSession/clearSession/tryRegisterSession` 同步写入 SQLite；`restoreSessions()` 启动时自动恢复 | 2026-06-12 |
| 03 | 并发安全无保护 | 🔴 | <span style="color:green">已解决</span> | 统一 `userSessions` Map 替代 `ipUsers` + `ipSessions` 双重存储，消除不一致性 | 2026-06-12 |
| 04 | 前端 ChatContext 职责过重 | 🟠 | <span style="color:green">已解决</span> | ChatContext.tsx 提取 `useMessageStore`（`client/src/hooks/useMessageStore.ts`）管理多会话消息存储、`useSessionNavigation`（`client/src/hooks/useSessionNavigation.ts`）管理导航栈；ChatContext.tsx 从 801 行降至 579 行 | 2026-06-12 |
| 05 | 前端 ref+state 双存储模式脆弱 | 🟠 | <span style="color:green">已解决</span> | 每个 `useState + useRef` 对替换为 `useReducer + 内部 ref`，reducer 管理 React 可见状态（FLUSH_MESSAGES/FLUSH_META/SET_TASK_MAP），ref 用于同步读写 | 2026-06-12 |
| 06 | taskCallToChild 顺序匹配不可靠 | 🟠 | <span style="color:green">已解决</span> | ChatContext.tsx 增加 DEV 警告日志：当子会话与 task 调用数量不匹配时输出告警 | 2026-06-12 |
| 07 | SSE 缓冲机制不可靠 | 🟠 | <span style="color:green">已解决</span> | `eventBuffer.js` MAX_BUFFER: 200→5000, BUFFER_TTL: 5m→30m | 2026-06-12 |
| 08 | 双前端维护成本高 | 🟠 | <span style="color:green">已解决</span> | index.js SPA 兜底改为检查 dist/index.html 是否存在，不存在时返回 503 错误提示而非过时的 Vanilla JS 页面 | 2026-06-12 |
| 09 | 同步路由与 SSE 事件流竞争条件 | 🟠 | <span style="color:green">已解决</span> | 删除 `POST /api/chat` 同步路由（死代码，前端只用 `/async`），消除竞争条件 + 减少 60 行死代码 | 2026-06-12 |
| 10 | SSE 多流订阅资源泄漏 | 🟠 | <span style="color:green">已解决</span> | `events.js` 新增 `AbortController`，客户端断开时 `controller.abort()` 通知 SDK 释放底层流资源；迭代中增加 `signal.aborted` 检查 | 2026-06-12 |
| 11 | SSE 事件类型用 switch-case 难维护 | 🟠 | <span style="color:green">已解决</span> | `events.js` 重构为 `eventHandlers` 分发映射表 + `LOGGED_EVENT_PREFIXES` 声明式过滤；新增事件类型只需注册处理函数 | 2026-06-12 |
| 12 | 与同类 OpenCode Web UI 的功能差距 | 🟠 | <span style="color:green">已解决</span> | 新增 PWA 支持：manifest.json、SVG 图标、Service Worker 离线缓存；index.html 添加 manifest+theme-color meta | 2026-06-12 |
| 13 | 日志 I/O 性能问题 | 🟡 | <span style="color:green">已解决</span> | `requestLogger.js` 跳过高频路径 `/api/events`、`/api/health` | 2026-06-12 |
| 14 | 配置中 web_search / web_fetch 被关闭 | 🟡 | <span style="color:green">已解决</span> | `config.json` 中 `web_search`/`web_fetch` 从 `false` 改为 `ask` | 2026-06-12 |
| 15 | 跨平台路径硬编码 | 🟡 | <span style="color:green">已解决</span> | `config.json` 移除 Windows 绝对路径 `directory` 字段 | 2026-06-12 |
| 16 | Provider 配置为空 | 🟡 | <span style="color:green">已解决</span> | `opencode.js` 新增 `checkProviderConfig()` 启动时检测常见 API Key 环境变量 | 2026-06-12 |
| 17 | statsService/analyticsService 职责重叠 | 🟡 | <span style="color:green">已解决</span> | `getStats()` 从 SQLite 聚合查询提问/反馈计数；`saveStats()`/`saveStatsSync()` 变为无操作；`recordMessage()` 同步更新 SQLite message_count；移除 `_stats.json` 读写 | 2026-06-12 |
| 18 | 缺少数据库迁移机制 | 🟡 | <span style="color:green">已解决</span> | `database.js` 新增 `_migrations` 表 + `applyMigrations()` 版本化执行：001 旧 ALTER TABLE 迁移至正式迁移系统，002 sessions 表、003 索引全部通过迁移管理 | 2026-06-12 |
| 19 | 无测试覆盖 | 🟡 | <span style="color:green">已解决</span> | 创建 `server/__tests__/api.test.js` 和 `setup.js`，package.json 新增 `test`/`test:watch`/`test:perf` 脚本 | 2026-06-12 |
| 20 | CSS Modules 与 Tailwind 混用 | 🟡 | <span style="color:green">已解决</span> | 删除 4 个 `.module.css` 文件（MessageBubble/MessageList/PartRenderer/ToolCallBlock），将所有 CSS Module 类名替换为 Tailwind v4 工具类 + CSS 变量 `var()` 引用 + 暗黑模式 `dark:` 变体 + `group-hover`/`open:` 状态变体 | 2026-06-12 | |
| 21 | 懒注册导致安全漏洞 | 🟡 | <span style="color:green">已解决</span> | `sessionGuard.js` 改用 `req.userId` 作为身份标识，移除 IP 兜底 | 2026-06-12 |
| 22 | 缺少类型安全共享层 | 🟡 | <span style="color:green">已解决</span> | 新增 `client/src/types/api-responses.ts` 定义 12 个 API 响应接口；前端所有 API 客户端 (stats/sessions/agents) 及组件（Dashboard/Sidebar/ChatContext）导入统一类型；消除 types/session.ts 冗余引用 | 2026-06-12 | |
| 23 | 缺少健康检查和监控端点 | 🟡 | <span style="color:green">已解决</span> | 新增 `server/routes/health.js`，暴露 `GET /api/health` | 2026-06-12 |
| 24 | 淘汰策略有缺陷 | 🟡 | <span style="color:green">已解决</span> | `userSessions` 统一淘汰，SQLite 同步清理 | 2026-06-12 |
| 25 | 性能测试框架缺失 | 🟡 | <span style="color:green">已解决</span> | 同一 #19，新增 `test:perf` 脚本 + 测试目录结构 | 2026-06-12 |
| 26 | 错误处理不够细化 | 🟡 | <span style="color:green">已解决</span> | `errorHandler.js` 新增 `AppError` 类 + 错误码 `code` + `isOperational` 分类 + 结构化 JSON 响应 | 2026-06-12 |
| 27 | 文档不完整 | 🟡 | <span style="color:green">已解决</span> | AGENTS.md 更新数据持久化、用户识别、中间件链、测试脚本章节 | 2026-06-12 |
| 28 | 缺少 CI/CD 集成 | 🟡 | <span style="color:green">已解决</span> | 创建 `.github/workflows/ci.yml`，push/PR 自动跑测试 + 前端构建 | 2026-06-12 |
| 29 | 缺少版本管理 | 🟡 | <span style="color:green">已解决</span> | 创建 `CHANGELOG.md`，Keep a Changelog + SemVer 格式 | 2026-06-12 |
| 30 | 性能监控缺失 | 🟡 | <span style="color:green">已解决</span> | 创建 `performanceLogger.js` 中间件，记录响应时间 + 慢请求告警（>1000ms），集成至 `app.js` 中间件链 | 2026-06-12 |

## 状态说明

| 状态 | 含义 |
|:----:|------|
| <span style="color:gray">待解决</span> | 尚未处理 |
| <span style="color:orange">进行中</span> | 正在修复 |
| <span style="color:green">已解决</span> | 已完成修复 |
| <span style="color:blue">已关闭</span> | 不修复或已废弃 |
