# OpenCode Chat — 项目架构说明

> 基于 [OpenCode SDK](https://github.com/opencode-ai/sdk) 的多用户 AI 咨询平台。用户通过 IP 自动识别（无需登录），每个用户独立管理会话。

---

## 目录

1. [项目概览](#1-项目概览)
2. [目录结构](#2-目录结构)
3. [技术栈](#3-技术栈)
4. [后端架构](#4-后端架构)
5. [前端架构](#5-前端架构)
6. [数据流](#6-数据流)
7. [部署与配置](#7-部署与配置)

---

## 1. 项目概览

| 属性 | 值 |
|------|-----|
| **项目名称** | OpenCode Chat |
| **项目类型** | AI 对话 Web 应用 |
| **后端** | Express.js 4.21 (ESM) |
| **前端** | React 19 + TypeScript 5.8 + Vite 6 + Tailwind CSS v4 |
| **AI 引擎** | OpenCode SDK (`@opencode-ai/sdk` ^1.14) |
| **数据存储** | 进程内存（Map），无数据库依赖 |
| **用户认证** | 无，基于 IP 自动识别 |
| **Node.js 版本** | >= 18（推荐 20+） |

---

## 2. 目录结构

```
opencode-chat/
├── package.json                 # 根 package — Express 服务入口 (ESM)
├── AGENTS.md                    # AI Agent 开发指引
├── server/                      # 后端 (Express.js + ESM)
│   ├── index.js                 # 服务入口
│   ├── app.js                   # Express app 工厂
│   ├── config.js                # 配置加载器
│   ├── config.json              # 配置源文件
│   ├── middleware/              # 中间件（6 个）
│   ├── routes/                  # 路由（8 组）
│   ├── services/                # 业务服务（4 个）
│   ├── storage/                 # 内存数据存储
│   └── logger/                  # 日志记录器
│
├── client/                      # 前端 (React 19 + TypeScript + Vite)
│   ├── package.json
│   ├── vite.config.ts           # Vite 配置
│   ├── tsconfig.json
│   ├── index.html               # SPA 入口 HTML
│   └── src/
│       ├── main.tsx             # React 入口
│       ├── App.tsx              # 根组件
│       ├── api/                 # API 客户端层（6 个文件）
│       ├── components/          # UI 组件
│       │   ├── layout/          # 布局组件（Sidebar、ChatArea）
│       │   ├── chat/            # 聊天核心组件（10 个）
│       │   ├── sidebar/         # 侧边栏组件（3 个）
│       │   ├── common/          # 通用组件（5 个）
│       │   └── ui/              # shadcn/ui 基础组件（9 个）
│       ├── contexts/            # React Context（ChatContext、ThemeContext）
│       ├── hooks/               # 自定义 hooks（3 个）
│       ├── lib/                 # 工具函数
│       ├── types/               # TypeScript 类型定义（3 个）
│       ├── utils/               # 工具函数（3 个）
│       └── styles/              # 全局样式
│
├── public/
│   └── index.html               # Vanilla JS 版 SPA（零构建 fallback）
│
├── dist/                        # 前端构建产物（gitignore）
│   └── index.html
│
└── logs/                        # 运行时日志（gitignore）
    ├── server.log               # 服务日志
    ├── server-*.log             # 归档日志
    └── _stats.json              # 统计数据快照
```

---

## 3. 技术栈

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | >= 18 | 运行时 |
| Express.js | ^4.21.0 | HTTP 框架 |
| `@opencode-ai/sdk` | ^1.14.29 | AI 引擎 SDK |

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| React | ^19.0.0 | UI 框架 |
| TypeScript | ~5.8.0 | 类型系统 |
| Vite | ^6.3.0 | 构建工具 |
| Tailwind CSS | ^4.3.0 | CSS 框架 |
| `react-markdown` | ^10.1.0 | Markdown 渲染 |
| `mermaid` | ^11.15.0 | 图表渲染 |
| `framer-motion` | ^12.40.0 | 动画 |
| `lucide-react` | ^1.16.0 | 图标库 |
| `radix-ui` / `@base-ui/react` | latest | 无障碍 UI 基元 |
| `@fontsource-variable/geist` | ^5.2.9 | Geist 可变字体 |
| `highlight.js` | ^11.11.1 | 代码高亮 |

---

## 4. 后端架构

### 4.1 请求处理链

```
客户端请求
    │
    ▼
express.json()           ← 解析 JSON 请求体
    │
    ▼
clientIPMiddleware       ← 解析客户端真实 IP（x-forwarded-for → x-real-ip → socket.remoteAddress）
    │                      source: server/middleware/clientIP.js
    ▼
requestLogger            ← 响应完成后记录访问日志（方法、路径、状态码、耗时、IP）
    │                      source: server/middleware/requestLogger.js
    ▼
rateLimiter              ← 仅作用于 /api 路径：200 次/15 分钟/IP；SSE 流豁免
    │                      source: server/middleware/rateLimiter.js
    ▼
routes (8 组)             ← 路由处理
    │                      source: server/routes/index.js
    ▼
errorHandler             ← 全局错误处理，捕获未处理异常
                           source: server/middleware/errorHandler.js
```

### 4.2 路由一览

| # | 路由文件 | 挂载路径 | 方法 | 端点 | 说明 |
|---|----------|----------|------|------|------|
| 1 | `sessions.js` | `/api/sessions` | POST | `/` | 创建会话（可选指定 agent） |
|   |          |          | GET | `/` | 获取当前用户会话列表 |
|   |          |          | POST | `/:id/abort` | 中断运行中的会话 |
| 2 | `chat.js` | `/api/chat` | POST | `/` | 同步发送（等待完整回复） |
|   |          |          | POST | `/async` | 异步发送（通过 SSE 接收回复） |
| 3 | `messages.js` | `/api/sessions` | GET | `/:id/messages` | 获取会话消息历史 |
| 4 | `feedback.js` | `/api/sessions` | POST | `/:id/feedback` | 提交满意度反馈 |
| 5 | `events.js` | `/api/events` | GET | `/` | SSE 事件流 |
| 6 | `stats.js` | `/api/stats` | GET | `/` | 平台统计数据 |
| 7 | `permission.js` | `/api/permission` | POST | `/respond` | 权限响应 |
|   |          |          | POST | `/question/reply` | 回复 AI 提问 |
|   |          |          | POST | `/question/reject` | 跳过/拒绝提问 |
| 8 | `agents.js` | `/api/agents` | GET | `/` | 可用 AI Agent 列表 |

### 4.3 服务层

```
server/services/
├── opencode.js           # OpenCode SDK 客户端管理
│   ├── startOpenCode()   # 调用 createOpencode() 启动 AI 引擎进程
│   ├── getClient()       # 获取 SDK client 实例
│   └── getServer()       # 获取 OpenCode server 信息
│
├── sessionService.js     # 会话生命周期管理
│   ├── createSession()   # 创建会话（调用 SDK + 更新本地状态）
│   ├── listSessions()    # 获取 IP 下的所有会话
│   ├── getSessionMeta()  # 获取会话元数据
│   ├── recordMessage()   # 递增消息计数
│   └── validateOwnership()# 验证会话归属
│   └── (定时器)           # 每小时清理过期会话（7 天 TTL）
│
├── userService.js        # 访客管理
│   ├── getClientIP()     # 从请求头解析 IP
│   └── ensureIP()        # 注册新访客
│
└── statsService.js       # 统计收集与持久化
    ├── incrementQuestions()
    ├── recordFeedback()
    ├── recordBlockedAccess()
    ├── getStats()
    └── saveStats()        # 异步写入 logs/_stats.json
```

### 4.4 数据存储

```js
// server/storage/store.js
export const ipUsers = new Map()       // ip → { sessionIds: Set }
export const ipSessions = new Map()    // ip → sessionId[]
export const sessionMeta = new Map()   // sessionId → { ip, createdAt, title, messageCount, agent }

export const stats = {
  visitors: new Set(),                  // 访客 IP 集合
  totalSessions: 0,
  totalQuestions: 0,
  satisfied: 0,
  unsatisfied: 0,
  blockedAccess: 0,
}
```

- 所有数据在进程内存中
- 重启后丢失（统计快照写入 `logs/_stats.json` 部分持久化）
- 会话 7 天未活跃自动清理

### 4.5 日志系统

```
server/logger/index.js
├── 输出目标：控制台（彩色） + 文件（logs/server.log）
├── 日志级别：INFO / WARN / ERROR / ACCESS
├── 自动归档：超过 10MB 自动压缩为 server-YYYYMMDD-HHmmss.log，保留 10 个归档
└── 启动时归档：如果已有 server.log 超过阈值，启动时自动归档
```

---

## 5. 前端架构

### 5.1 组件树

```
<main.tsx>
  └── <StrictMode>
      └── <ErrorBoundary>
          └── <ThemeProvider>
              └── <ChatProvider>
                  └── <App>
                      ├── <ErrorBanner />           ← 全局错误提示
                      ├── <Sidebar>                  ← 侧边栏
                      │   ├── <SidebarHeader />      ← 标题 + 新建对话按钮
                      │   └── <SessionList>          ← 会话列表
                      │       └── <SessionItem />    ← 每条会话
                      └── <ChatArea>                 ← 主聊天区域
                          ├── <ChatHeader />          ← 顶部标题栏 + 菜单按钮
                          ├── <AgentSelector />       ← 首页 Agent 选择（无会话时）
                          ├── <MessageList>           ← 消息列表
                          │   └── <MessageBubble>
                          │       ├── <PartRenderer>
                          │       │   ├── <MarkdownRenderer />   ← Markdown + 代码高亮 + 图表
                          │       │   ├── <ReasoningBlock />     ← 推理过程折叠块
                          │       │   └── <ToolCallBlock />      ← 工具调用状态
                          │       ├── <FeedbackRow />            ← 满意/不满意按钮
                          │       └── <TypingIndicator />        ← 打字动画
                          └── <ChatInput />           ← 输入框 + 发送/中断按钮
                  │
                  └── <PermissionDialog />  ← AI 权限/问题对话框（Portal）
```

### 5.2 状态管理层

```
React Context 体系
├── ThemeContext          ← 主题状态
│   ├── theme: 'light' | 'dark'
│   ├── toggle()
│   └── 自动跟随系统偏好 + localStorage 持久化
│
└── ChatContext           ← 核心应用状态
    ├── 会话：sessions, sessionsLoading, isCreating
    ├── 消息：allMessages (Map<sessionId, ChatMessage[]>), messagesLoading
    ├── 流式：isStreaming
    ├── 输入：inputValue
    ├── UI：sidebarOpen, globalError
    ├── Agent：agents, agentsLoading, selectedAgent
    ├── 多会话导航：navigationStack, parentSessionId
    │   ├── navigateToSession(sessionId)   ← 导航到子会话
    │   ├── navigateBack()                 ← 返回上一会话
    │   └── navigateToParent()             ← 返回父会话
    ├── 反馈：feedbackStates, submitFeedback()
    └── 权限：PendingPermission → PermissionDialog
```

### 5.3 SSE 事件流

```
客户端 useEvents hook (自动重连 + 指数退避 1s→30s)
    │
    ▼
GET /api/events (EventSource)
    │
    ▼
事件路由 (20+ 种事件类型)
├── message.updated          → 存储消息到对应会话
├── message.part.updated     → 更新消息部件
├── message.part.delta       → 文本增量追加
├── session.status           → 控制 isStreaming
├── session.created/updated  → 记录子会话元数据
├── session.next.tool.*      → 实时工具调用展示
├── session.next.reasoning.* → 推理过程展示
├── session.next.shell.*     → Shell 执行展示
├── permission.asked         → 弹出权限对话框
└── question.asked           → 弹出问题多步表单
```

### 5.4 前端关键路径

```
发送消息（异步模式）：
用户输入 → ChatInput.onSend()
    → ChatContext.sendMessage()
        → 追加 user message 到本地 allMessages
        → chatApi.sendMessageAsync()  →  POST /api/chat/async
        → 返回 { ok: true }
    → AI 通过 SSE 事件流逐步推送回复
        → message.updated / message.part.delta / message.part.updated
        → ChatContext 中对应 handler 更新 allMessages
        → React 重新渲染 MessageList
    → AI 回复完成
        → session.status → isStreaming = false
```

---

## 6. 数据流

### 6.1 完整请求链路

```
用户操作 (浏览器)
    │
    ├── HTTP 请求 ──→ Express 服务器 (:3000)
    │                      │
    │                      ├── 中间件处理 (IP 解析/日志/限流)
    │                      │
    │                      ├── 路由处理
    │                      │   ├── 业务逻辑 (sessionService/statsService)
    │                      │   └── SDK 调用 → OpenCode AI 引擎 (:4096)
    │                      │
    │                      ├── 响应 JSON ──→ 浏览器渲染
    │                      │
    │                      └── 错误处理 (如有)
    │
    └── SSE 连接 ──→ Express 服务器 (:3000)
                         │
                         ├── 订阅 OpenCode SDK event.subscribe()
                         │
                         └── 实时推送事件 → 浏览器 useEvents hook
                              → ChatContext state → React 渲染
```

### 6.2 用户与会话生命周期

```
新访客访问
    │
    ▼
GET /api/sessions → ensureIP() 注册新访客
    │
    ▼
POST /api/sessions → createSession()
    ├── OpenCode SDK: client.session.create()
    ├── 本地记录: sessionMeta.set(id, { ip, createdAt, title, messageCount, agent })
    └── 返回: { sessionId, title, agent }
    │
    ▼
POST /api/chat/async → 发送消息
    ├── OpenCode SDK: client.session.promptAsync()
    ├── stats: totalQuestions++
    └── SSE 逐步接收 AI 回复
    │
    ▼
7 天未活跃
    │
    ▼
cleanupExpiredSessions() 定时器 → 自动删除
```

---

## 7. 部署与配置

### 7.1 配置源

| 层级 | 文件 | 说明 |
|------|------|------|
| 默认值 | `server/config.json` | 所有配置项的默认值 |
| 环境覆盖 | 环境变量 | `PORT` / `HOSTNAME` / `NODE_ENV` / `MODEL` |
| SDK 配置 | `config.js` → `buildOpenCodeConfig()` | 传递给 `createOpencode()` |

### 7.2 完整配置项

```jsonc
{
  "server": {
    "port": 3000,          // 环境变量 PORT 覆盖
    "hostname": "0.0.0.0"  // 环境变量 HOSTNAME 覆盖
  },
  "opencode": {
    "port": 4096,           // 固定，不可覆盖
    "hostname": "127.0.0.1" // 固定
  },
  "model": "deepseek/deepseek-chat",  // 环境变量 MODEL 覆盖
  "small_model": "deepseek/deepseek-chat",
  "logLevel": "INFO",
  "autoupdate": false,
  "agent": { "build": { "temperature": 0.7 } },
  "tools": {
    "bash": true, "write": true, "read": true,
    "edit": true, "search": true,
    "web_search": false, "web_fetch": false
  },
  "compaction": { "auto": true, "prune": true, "tail_turns": 8, ... },
  "tool_output": { "max_lines": 500, "max_bytes": 102400 },
  "snapshot": true,
  "agentOptions": [ /* AI Agent 选项列表 */ ],
  "provider": {},
  "log": { "maxFileSize": 10485760, "maxArchives": 10 }
}
```

### 7.3 两个前端版本

| 版本 | 位置 | 技术栈 | 触发条件 |
|------|------|--------|----------|
| **React SPA**（主要） | `dist/index.html` | React + TypeScript + Vite | 构建后存在 `dist/index.html` |
| **Vanilla JS**（fallback） | `public/index.html` | 原生 JS + CSS | `dist/index.html` 不存在 |

服务启动时自动检测 `dist/index.html` 是否存在，优先级：dist > public。

---

## 附录：关键文件索引

| 文件 | 行数 | 职责 |
|------|------|------|
| `server/index.js` | ~60 | 服务入口，启动 SDK + HTTP |
| `server/app.js` | 17 | Express app 工厂 |
| `server/config.js` | ~45 | 配置加载 |
| `server/services/opencode.js` | ~55 | OpenCode SDK 管理 |
| `server/services/sessionService.js` | ~80 | 会话 CRUD + 清理 |
| `server/services/statsService.js` | ~85 | 统计收集 + 持久化 |
| `server/services/userService.js` | ~30 | 访客管理 |
| `server/middleware/rateLimiter.js` | 39 | 限流器 |
| `server/middleware/sessionGuard.js` | 24 | 会话权限校验 |
| `server/routes/chat.js` | ~115 | 核心对话路由 |
| `server/routes/events.js` | ~80 | SSE 事件流 |
| `client/src/contexts/ChatContext.tsx` | 773 | 全局状态管理 |
| `client/src/hooks/useEvents.ts` | 199 | SSE 事件订阅 |
| `client/src/App.tsx` | ~100 | 根组件编排 |
