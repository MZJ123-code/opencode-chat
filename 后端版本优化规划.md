# OpenCode Chat — 版本优化规划

> 基于后端代码分析的优化路线图。按优先级分为 P0（阻塞/正确性）、P1（性能/架构）、P2（代码整洁）三级。

---

## P0 — 必须修复

### 0.1 退出流程中 `saveStats()` 重复调用

**问题**：`server/index.js` 的 `shutdown()` 调用一次 `saveStats()`，同时 `process.on("uncaughtException")` 又调用一次。`saveStats` 内部有 `pending` 防抖布尔值，由于 `setImmediate` 异步执行，exit 时可能导致写入未完成就被进程退出打断，数据丢失。

**方案**：shutdown 中将 `saveStats()` 改为同步执行（`fs.writeFileSync`），并且只调用一次：

```js
function shutdown() {
  logger.info("正在关闭服务...")
  flushStatsSync() // 同步写入，确保退出前落盘
  getServer().close?.()
  process.exit(0)
}
```

**影响范围**：`server/index.js`、`server/services/statsService.js`

---

### 0.2 内存无上限保护

**问题**：所有数据存储在进程内存的 `Map` 中，`sessionMeta` 虽然有 7 天 TTL 清理，但如果攻击者短时间内大量创建会话（POST /api/sessions 无频率限制之外的更多限制），内存会持续增长直到 OOM。单会话消息条数也无上限。

**方案**：
1. 给 `sessionMeta` 增加硬上限（如 1000 条），超出后 LRU 淘汰
2. 配置中增加 `maxSessionsPerIP` 限制

**影响范围**：`server/services/sessionService.js`、`server/config.json`

---

### 0.3 SSE 断连后事件丢失

**问题**：前端 SSE 断连后，后端没有缓存机制，OpenCode SDK 的事件流无法重置游标。AI 正在回复时断连会导致回复彻底丢失，用户必须重新发送消息。

**方案**：短期可在前端增加重连后的自动重试（发送一条 GET 消息历史拉取最近消息）；长期后端可引入内存事件环形缓冲区（如保留最后 100 条事件），新 SSE 连接时回放未消费事件。

**影响范围**：`server/routes/events.js`、`client/src/hooks/useEvents.ts`

---

### 0.4 `config.json` 重复 agent 条目

**问题**：第 63-67 行的 `"门子俊"`（agent: "explore"）与上方 `"代码探索"` 完全重复，导致前端 Agent 选择页出现两个功能完全相同的选项。

**方案**：删除多余的 agent 条目。

**影响范围**：`server/config.json:63-67`

---

## P1 — 建议优化

### 1.1 限流器剥离为独立包

**问题**：手写的限流器是单进程内存实现，PM2 cluster 下各自独立计数，限流失效。清理间隔 5min 但窗口 15min，清理粒度不精确。

**方案**：替换为 `express-rate-limit`（成熟包，支持外部 store）：

```js
import rateLimit from 'express-rate-limit'
export const apiLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 200,
  standardHeaders: true,
  legacyHeaders: false,
  keyGenerator: (req) => req.clientIP || 'unknown',
  skip: (req) => req.path === '/events' && req.headers.accept === 'text/event-stream',
})
```

**影响范围**：`server/middleware/rateLimiter.js`（可从约 50 行降到 15 行）、`package.json`（新增依赖）

**收益**：代码减少 70%，支持 Redis store 实现跨进程共享限流，标准 RateLimit 响应头。

---

### 1.2 统计写入改为同步 + 启动时恢复

**问题**：`saveStats()` 使用 `setImmediate` + `pending` 防抖，高并发下多条消息只有最后一次写入生效，中间计数丢失。进程崩溃时统计不准确。

**方案**：
1. 启动时从 `logs/_stats.json` 恢复 `stats` 对象（如果存在）
2. 每次变更时同步写入（`fs.writeFileSync` 或 `fsync`）
3. 或在写入频率和可靠性间取平衡：`debounce(2000)` + 每 10 秒定期落盘

**影响范围**：`server/services/statsService.js`、`server/index.js`

---

### 1.3 会话元数据访问日志应脱敏

**问题**：`sessionGuard.js:14-18` 在拒绝跨 IP 访问时，日志中记录了被拒绝会话的 `session_agent`、`session_title`。虽然没返回给用户，但日志中包含了非当前用户的会话信息。

**方案**：日志中移除 `session_agent` 和 `session_title`，仅记录 sessionId：

```js
logger.warn(`访问被拒绝: ${ip} -> ${sessionId}`, {
  session_exists: !!meta,
})
```

**影响范围**：`server/middleware/sessionGuard.js`

---

### 1.4 routes 挂载路径统一

**问题**：`routes/index.js` 中 `sessionsRouter` 和 `messagesRouter` 都挂载在 `/api/sessions`；`sessions.js` 里混合了 POST/GET 会话和 `/abort` 端点，职责不够单一。

**方案**：将 abort 端点抽到独立的 `abort.js`，messages 路由显式挂载在 `/api/sessions/:id/messages`：

```js
// routes/index.js
app.use("/api/sessions", sessionsRouter)
app.use("/api/sessions/:id/messages", messagesRouter)
app.use("/api/sessions", feedbackRouter)
app.use("/api/sessions/:id/abort", abortRouter)
```

**影响范围**：`server/routes/index.js`、`server/routes/sessions.js`（移除 abort）

---

### 1.5 OpenCode 配置覆盖策略统一

**问题**：`PORT` / `HOSTNAME` / `MODEL` 通过环境变量覆盖，但 `tools` / `compaction` / `agent` 等只能改 `config.json`。没有统一的合并逻辑。

**方案**：在 `config.js` 中增加对环境变量 `OPENCODE_TOOLS`（JSON 字符串）等的支持，或统一规定所有运行时配置均可从环境变量覆盖。

**影响范围**：`server/config.js`

---

## P2 — 可选的代码整洁

### 2.1 删除死代码

| 文件 | 内容 | 原因 |
|------|------|------|
| `server/services/statsService.js:6` | `recordVisit()` | 从未被调用 |
| `server/services/statsService.js:10` | `incrementSessions()` | 从未被调用 |
| `server/services/userService.js:25` | `getVisitorInfo()` | 从未被引入 |

### 2.2 配置默认值集中管理

`config.json` 中有默认值，`config.js` 中也有 fallback（`|| "unknown"`），两层默认值。建议默认值只在一个地方维护，优先用 config.json，环境变量覆盖。

### 2.3 `opencode.js` 的 getServer() 类型安全

`server/services/opencode.js:13` 中 `_server = result.server`，类型是 `OpenCodeServer`。但 `index.js` 的 `shutdown()` 中 `server.close?.()` 未验证类型。建议强类型 + 非空断言。

---

## 版本建议

| 版本 | 包含内容 | 工作量 |
|------|----------|--------|
| **v1.1** (hotfix) | P0.4 删除重复 agent + P1.3 日志脱敏 + P2.1 删死代码 | ~0.5 天 |
| **v1.2** (可靠性) | P0.1 退出流程 + P0.2 内存上限 + P1.2 统计写入同步 | ~1 天 |
| **v1.3** (架构) | P1.1 替换限流器 + P1.4 路由统一 + P1.5 配置覆盖策略 | ~1 天 |
| **v2.0** (流式) | P0.3 SSE 重连恢复 + 事件缓冲区 | ~2 天 |
